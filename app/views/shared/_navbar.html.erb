<div class="navbar">
  <div class="navbar-inner">
    <ul class="nav">
      <% if user_signed_in? %>
        <li class="active"><a href=<%= user_path(current_user) %>>home page</a></li>
        <li><%= link_to "sign out", destroy_user_session_path, method: :delete %><li>
      <% else %>
        <li><%= link_to "sign in", new_user_session_path %></li>
        <li><%= link_to "sign up", new_user_registration_path %></li>
      <% end %>

      <li><%= link_to "stores", stores_path %></li>
      <li><%= link_to "products", products_path %></li>
      <% if user_signed_in? %>
        <li>
          <a href="#new-product" role="button" data-toggle="modal">add new product</a>
        </li>
        <li>
          <a href="#new-store" role="button" data-toggle="modal">add new store</a>
        </li>
      <% end %>
    </ul>
  </div>
</div>

