<h1>Review <%= @product_review.product.name %></h1>

<%= simple_form_for @product_review do |f| %>

  <%= f.input :product_id, as: :hidden %>
  <%= f.input :rating, as: :radio_buttons, collection: 1..7 %>
  <%= f.input :body, as: :text %>
  <%= f.button :submit %>
<% end %>
<%= current_user.id %>